import { Command } from 'nestjs-command';
import { Injectable } from '@nestjs/common';
import { BaseMakeCommand } from '@nesvel/nestjs-console';

/**
 * <%= name %> Command
 *
 * @description <%= description || 'Command description' %>
 * @author Generated by @nesvel/nestjs-console
 *
 * @example
 * ```bash
 * npm run console <%= commandName %> <argument>
 * ```
 */
@Injectable()
export class <%= name %> extends BaseMakeCommand {
  /**
   * Execute the <%= commandName %> command
   *
   * @param name - The name argument
   * @returns Promise that resolves when command completes
   */
  @Command({
    command: '<%= commandName %> <name>',
    describe: '<%= description || "Command description" %>',
  })
  async execute(name: string): Promise<void> {
    await this.generateFromStub(name, {
      stubName: '<%= stubName %>',
      outputDir: '<%= outputDir %>',
    });
  }
}
