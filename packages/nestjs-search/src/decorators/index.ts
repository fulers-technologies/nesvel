/**
 * Decorators Module
 *
 * Exports all decorator functions used in the nestjs-search package.
 *
 * The InjectSearchService decorator is the recommended way to inject
 * the SearchService into your controllers and services.
 *
 * @author Nesvel
 * @since 1.0.0
 */

export * from './inject-search-service.decorator';
