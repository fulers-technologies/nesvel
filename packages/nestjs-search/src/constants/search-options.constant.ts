/**
 * Search options injection token
 *
 * Used to inject SearchConfig configuration into the subscriber.
 * This allows the subscriber to access configuration like autoSync and indexPrefix.
 *
 * @constant
 * @type {string}
 */
export const SEARCH_OPTIONS = 'SEARCH_OPTIONS';
