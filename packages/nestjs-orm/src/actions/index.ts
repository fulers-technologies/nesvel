/**
 * Actions
 *
 * Action classes for encapsulating business logic following the Command pattern.
 * Actions provide a clean way to organize complex business logic outside of controllers and services.
 */

export * from './base.action';
