/**
 * <%= enumName %>
 *
 * TypeScript enum for <%= description || fileName.replace(/-/g, ' ') %>
 *
 * @description <%= enumName %> enum definition
 * @author Generated by @nesvel/nestjs-orm
 * @since 1.0.0
 *
 * @example
 * ```typescript
 * // Use in entity:
 * import { <%= enumName %> } from '@/enums/<%= fileName %>.enum';
 *
 * @Entity()
 * export class MyEntity extends BaseEntity {
 *   @Property({ type: 'enum' })
 *   @Enum(() => <%= enumName %>)
 *   status: <%= enumName %> = <%= enumName %>.ACTIVE;
 * }
 * ```
 */
export enum <%= enumName %> {
  // TODO: Define enum values
  // Example for status enum:
  // ACTIVE = 'active',
  // INACTIVE = 'inactive',
  // PENDING = 'pending',
  // SUSPENDED = 'suspended',
}

/**
 * Get all enum values as an array
 *
 * @returns Array of all enum values
 */
export function get<%= enumName %>Values(): <%= enumName %>[] {
  return Object.values(<%= enumName %>) as <%= enumName %>[];
}

/**
 * Check if a value is a valid <%= enumName %>
 *
 * @param value - Value to check
 * @returns True if value is valid enum member
 */
export function isValid<%= enumName %>(value: any): value is <%= enumName %> {
  return Object.values(<%= enumName %>).includes(value);
}

/**
 * Get enum value labels for display
 * Override this function to provide human-readable labels
 *
 * @param value - Enum value
 * @returns Human-readable label
 */
export function get<%= enumName %>Label(value: <%= enumName %>): string {
  // TODO: Implement custom labels
  // Example:
  // const labels: Record<<%= enumName %>, string> = {
  //   [<%= enumName %>.ACTIVE]: 'Active',
  //   [<%= enumName %>.INACTIVE]: 'Inactive',
  // };
  // return labels[value] || value;

  return value.toString();
}
