import { Module } from '@nestjs/common';
import { OrmModule } from '@nesvel/nestjs-orm';
import { <%= modelName %> } from '@/entities/<%= fileName %>';
import { <%= repositoryName %> } from '@/repositories/<%= repositoryFileName %>';
import { <%= serviceName %> } from '@/services/<%= serviceFileName %>';
import { <%= controllerName %> } from '@/controllers/<%= controllerFileName %>';

/**
 * <%= name %> Module
 *
 * @description Module for <%= modelName %> feature
 * @author Generated by @nesvel/nestjs-orm
 */
@Module({
  imports: [
    OrmModule.forFeature([<%= modelName %>]),
  ],
  controllers: [<%= controllerName %>],
  providers: [
    <%= repositoryName %>,
    <%= serviceName %>,
  ],
  exports: [
    <%= serviceName %>,
  ],
})
export class <%= name %> {}
