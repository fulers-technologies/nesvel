import {
  Controller,
  Get,
  Post,
  Put,
  Patch,
  Delete,
  Body,
  Param,
  Query,
  HttpCode,
  HttpStatus,
  ParseIntPipe,
  NotFoundException,
  BadRequestException,
  UseInterceptors,
  ClassSerializerInterceptor,
} from '@nestjs/common';
import {
  ApiTags,
  ApiOperation,
  ApiResponse,
  ApiParam,
  ApiQuery,
  ApiBody,
  ApiCreatedResponse,
  ApiOkResponse,
  ApiNotFoundResponse,
  ApiBadRequestResponse,
  ApiInternalServerErrorResponse,
} from '@nesvel/nestjs-swagger';
import { <%= serviceName %> } from '@/services/<%= serviceFileName %>';
import { <%= modelName %> } from '@/entities/<%= fileName %>';
// import { Create<%= modelName %>Dto } from '@/dtos/create-<%= fileName %>.dto';
// import { Update<%= modelName %>Dto } from '@/dtos/update-<%= fileName %>.dto';

/**
 * <%= name %> Controller
 *
 * RESTful API controller for managing <%= modelName %> resources.
 * Provides full CRUD operations with OpenAPI documentation.
 *
 * @description REST API controller for <%= modelName %> entity
 * @author Generated by @nesvel/nestjs-orm
 */
@ApiTags('<%= routePath %>')
@Controller('<%= routePath %>')
@UseInterceptors(ClassSerializerInterceptor)
export class <%= name %> {
  /**
   * Creates an instance of <%= name %>
   *
   * @param <%= serviceVar %> - The <%= modelName %> service
   */
  constructor(private readonly <%= serviceVar %>: <%= serviceName %>) {}

  /**
   * Get all <%= modelName.toLowerCase() %>s with optional pagination
   *
   * Retrieves a list of all <%= modelName.toLowerCase() %>s. Supports pagination via query parameters.
   *
   * @param page - Page number (1-based)
   * @param limit - Number of items per page
   * @returns List of <%= modelName.toLowerCase() %>s or paginated result
   */
  @Get()
  @HttpCode(HttpStatus.OK)
  @ApiOperation({
    summary: 'Get all <%= modelName.toLowerCase() %>s',
    description: 'Retrieve a list of all <%= modelName.toLowerCase() %>s with optional pagination',
  })
  @ApiQuery({
    name: 'page',
    required: false,
    type: Number,
    description: 'Page number for pagination (1-based)',
    example: 1,
  })
  @ApiQuery({
    name: 'limit',
    required: false,
    type: Number,
    description: 'Number of items per page',
    example: 20,
  })
  @ApiOkResponse({
    description: 'List of <%= modelName.toLowerCase() %>s retrieved successfully',
    type: [<%= modelName %>],
  })
  @ApiInternalServerErrorResponse({
    description: 'Internal server error',
  })
  async findAll(
    @Query('page', new ParseIntPipe({ optional: true })) page?: number,
    @Query('limit', new ParseIntPipe({ optional: true })) limit?: number,
  ) {
    if (page && limit) {
      return this.<%= serviceVar %>.paginate(page, limit);
    }
    return this.<%= serviceVar %>.all();
  }

  /**
   * Get a single <%= modelName.toLowerCase() %> by ID
   *
   * Retrieves a specific <%= modelName.toLowerCase() %> by its unique identifier.
   *
   * @param id - The <%= modelName.toLowerCase() %> ID
   * @returns The <%= modelName.toLowerCase() %> entity
   * @throws NotFoundException if <%= modelName.toLowerCase() %> not found
   */
  @Get(':id')
  @HttpCode(HttpStatus.OK)
  @ApiOperation({
    summary: 'Get <%= modelName.toLowerCase() %> by ID',
    description: 'Retrieve a specific <%= modelName.toLowerCase() %> by its unique identifier',
  })
  @ApiParam({
    name: 'id',
    type: Number,
    description: 'The unique identifier of the <%= modelName.toLowerCase() %>',
    example: 1,
  })
  @ApiOkResponse({
    description: '<%= modelName %> found and retrieved successfully',
    type: <%= modelName %>,
  })
  @ApiNotFoundResponse({
    description: '<%= modelName %> not found',
  })
  @ApiBadRequestResponse({
    description: 'Invalid ID format',
  })
  async findOne(@Param('id', ParseIntPipe) id: number) {
    try {
      return await this.<%= serviceVar %>.findOrFail(id);
    } catch (error: Error | any) {
      throw new NotFoundException(`<%= modelName %> with ID ${id} not found`);
    }
  }

  /**
   * Create a new <%= modelName.toLowerCase() %>
   *
   * Creates a new <%= modelName.toLowerCase() %> resource with the provided data.
   *
   * @param data - The <%= modelName.toLowerCase() %> creation data
   * @returns The created <%= modelName.toLowerCase() %>
   * @throws BadRequestException if validation fails
   */
  @Post()
  @HttpCode(HttpStatus.CREATED)
  @ApiOperation({
    summary: 'Create a new <%= modelName.toLowerCase() %>',
    description: 'Create a new <%= modelName.toLowerCase() %> resource with the provided data',
  })
  @ApiBody({
    description: '<%= modelName %> creation data',
    // type: Create<%= modelName %>Dto,
    schema: {
      type: 'object',
      properties: {
        name: { type: 'string', example: 'Example Name' },
        // Add more properties based on your DTO
      },
    },
  })
  @ApiCreatedResponse({
    description: '<%= modelName %> created successfully',
    type: <%= modelName %>,
  })
  @ApiBadRequestResponse({
    description: 'Invalid input data or validation failed',
  })
  @ApiInternalServerErrorResponse({
    description: 'Internal server error',
  })
  async create(@Body() data: Partial<<%= modelName %>>) {
    // TODO: Replace with DTO type after creating it
    // async create(@Body() data: Create<%= modelName %>Dto) {
    try {
      return await this.<%= serviceVar %>.create(data);
    } catch (error: Error | any) {
      throw new BadRequestException(
        `Failed to create <%= modelName.toLowerCase() %>: ${error.message}`,
      );
    }
  }

  /**
   * Update an existing <%= modelName.toLowerCase() %> (full update)
   *
   * Replaces all fields of an existing <%= modelName.toLowerCase() %> with the provided data.
   *
   * @param id - The <%= modelName.toLowerCase() %> ID
   * @param data - The complete updated <%= modelName.toLowerCase() %> data
   * @returns The updated <%= modelName.toLowerCase() %>
   * @throws NotFoundException if <%= modelName.toLowerCase() %> not found
   * @throws BadRequestException if validation fails
   */
  @Put(':id')
  @HttpCode(HttpStatus.OK)
  @ApiOperation({
    summary: 'Update <%= modelName.toLowerCase() %> (full update)',
    description: 'Replace all fields of an existing <%= modelName.toLowerCase() %> with provided data',
  })
  @ApiParam({
    name: 'id',
    type: Number,
    description: 'The unique identifier of the <%= modelName.toLowerCase() %>',
    example: 1,
  })
  @ApiBody({
    description: 'Complete <%= modelName.toLowerCase() %> update data',
    // type: Update<%= modelName %>Dto,
    schema: {
      type: 'object',
      properties: {
        name: { type: 'string', example: 'Updated Name' },
        // Add more properties based on your DTO
      },
    },
  })
  @ApiOkResponse({
    description: '<%= modelName %> updated successfully',
    type: <%= modelName %>,
  })
  @ApiNotFoundResponse({
    description: '<%= modelName %> not found',
  })
  @ApiBadRequestResponse({
    description: 'Invalid input data or validation failed',
  })
  async update(
    @Param('id', ParseIntPipe) id: number,
    @Body() data: Partial<<%= modelName %>>,
    // Replace with: @Body() data: Update<%= modelName %>Dto,
  ) {
    try {
      return await this.<%= serviceVar %>.update(id, data);
    } catch (error: Error | any) {
      if (error.message.includes('not found')) {
        throw new NotFoundException(`<%= modelName %> with ID ${id} not found`);
      }
      throw new BadRequestException(
        `Failed to update <%= modelName.toLowerCase() %>: ${error.message}`,
      );
    }
  }

  /**
   * Partially update an existing <%= modelName.toLowerCase() %>
   *
   * Updates only the specified fields of an existing <%= modelName.toLowerCase() %>.
   *
   * @param id - The <%= modelName.toLowerCase() %> ID
   * @param data - The partial <%= modelName.toLowerCase() %> update data
   * @returns The updated <%= modelName.toLowerCase() %>
   * @throws NotFoundException if <%= modelName.toLowerCase() %> not found
   * @throws BadRequestException if validation fails
   */
  @Patch(':id')
  @HttpCode(HttpStatus.OK)
  @ApiOperation({
    summary: 'Partially update <%= modelName.toLowerCase() %>',
    description: 'Update only specified fields of an existing <%= modelName.toLowerCase() %>',
  })
  @ApiParam({
    name: 'id',
    type: Number,
    description: 'The unique identifier of the <%= modelName.toLowerCase() %>',
    example: 1,
  })
  @ApiBody({
    description: 'Partial <%= modelName.toLowerCase() %> update data',
    schema: {
      type: 'object',
      properties: {
        name: { type: 'string', example: 'Updated Name' },
        // Add more properties - all optional for PATCH
      },
    },
  })
  @ApiOkResponse({
    description: '<%= modelName %> updated successfully',
    type: <%= modelName %>,
  })
  @ApiNotFoundResponse({
    description: '<%= modelName %> not found',
  })
  @ApiBadRequestResponse({
    description: 'Invalid input data or validation failed',
  })
  async partialUpdate(
    @Param('id', ParseIntPipe) id: number,
    @Body() data: Partial<<%= modelName %>>,
  ) {
    try {
      return await this.<%= serviceVar %>.update(id, data);
    } catch (error: Error | any) {
      if (error.message.includes('not found')) {
        throw new NotFoundException(`<%= modelName %> with ID ${id} not found`);
      }
      throw new BadRequestException(
        `Failed to update <%= modelName.toLowerCase() %>: ${error.message}`,
      );
    }
  }

  /**
   * Delete a <%= modelName.toLowerCase() %>
   *
   * Permanently deletes a <%= modelName.toLowerCase() %> from the database.
   *
   * @param id - The <%= modelName.toLowerCase() %> ID
   * @returns Deletion confirmation
   * @throws NotFoundException if <%= modelName.toLowerCase() %> not found
   */
  @Delete(':id')
  @HttpCode(HttpStatus.NO_CONTENT)
  @ApiOperation({
    summary: 'Delete <%= modelName.toLowerCase() %>',
    description: 'Permanently delete a <%= modelName.toLowerCase() %> from the database',
  })
  @ApiParam({
    name: 'id',
    type: Number,
    description: 'The unique identifier of the <%= modelName.toLowerCase() %>',
    example: 1,
  })
  @ApiResponse({
    status: HttpStatus.NO_CONTENT,
    description: '<%= modelName %> deleted successfully',
  })
  @ApiNotFoundResponse({
    description: '<%= modelName %> not found',
  })
  @ApiInternalServerErrorResponse({
    description: 'Internal server error',
  })
  async delete(@Param('id', ParseIntPipe) id: number) {
    const deleted = await this.<%= serviceVar %>.delete(id);
    if (!deleted) {
      throw new NotFoundException(`<%= modelName %> with ID ${id} not found`);
    }
    return { message: '<%= modelName %> deleted successfully' };
  }
}
