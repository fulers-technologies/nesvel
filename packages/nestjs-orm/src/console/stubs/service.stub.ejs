import { Injectable } from '@nestjs/common';
import { BaseService } from '@nesvel/nestjs-orm';
import { <%= modelName %> } from '@/entities/<%= fileName %>';
import { <%= repositoryName %> } from '@/repositories/<%= repositoryFileName %>';

/**
 * <%= name %> Service
 *
 * @description Service layer for <%= modelName %> entity business logic
 * @extends BaseService<<%= modelName %>>
 * @author Generated by @nesvel/nestjs-orm
 */
@Injectable()
export class <%= name %> extends BaseService<<%= modelName %>> {
  /**
   * Creates an instance of <%= name %>
   *
   * @param <%= repositoryVar %> - The <%= modelName %> repository
   */
  constructor(private readonly <%= repositoryVar %>: <%= repositoryName %>) {
    super(<%= repositoryVar %>);
  }

  /**
   * Add custom service methods here
   *
   * @example
   * async findByEmail(email: string): Promise<<%= modelName %> | null> {
   *   return this.<%= repositoryVar %>.first({ email });
   * }
   *
   * @example
   * async activate(id: number): Promise<<%= modelName %>> {
   *   return this.<%= repositoryVar %>.update(id, { isActive: true });
   * }
   *
   * @example
   * async processBusinessLogic(data: Partial<<%= modelName %>>): Promise<<%= modelName %>> {
   *   // Complex business logic here
   *   const entity = await this.<%= repositoryVar %>.create(data);
   *   // Additional processing
   *   return entity;
   * }
   */
}
