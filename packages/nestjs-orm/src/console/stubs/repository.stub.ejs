import { Injectable } from '@nestjs/common';
import {
  EntityManager,
  EntityRepository,
  InjectRepository,
  BaseRepository,
} from '@nesvel/nestjs-orm';
import { <%= modelName %> } from '@/entities/<%= fileName %>';

/**
 * <%= name %> Repository
 *
 * @description Repository for <%= modelName %> entity
 * @extends BaseRepository<<%= modelName %>>
 * @author Generated by @nesvel/nestjs-orm
 */
@Injectable()
export class <%= name %> extends BaseRepository<<%= modelName %>> {
  /**
   * Creates an instance of <%= name %>
   *
   * @param repository - The <%= modelName %> entity repository
   * @param em - The entity manager
   */
  constructor(
    @InjectRepository(<%= modelName %>) repository: EntityRepository<<%= modelName %>>,
    em: EntityManager
  ) {
    super(em, repository);
  }

  /**
   * Add custom repository methods here
   *
   * @example
   * async findByEmail(email: string): Promise<<%= modelName %> | null> {
   *   return this.first({ email });
   * }
   *
   * @example
   * async findActive(): Promise<<%= modelName %>[]> {
   *   return this.where({ isActive: true });
   * }
   *
   * @example
   * async findWithRelations(id: number): Promise<<%= modelName %> | null> {
   *   return this.getRepository().findOne(id, {
   *     populate: ['relation1', 'relation2'],
   *   });
   * }
   */
}
