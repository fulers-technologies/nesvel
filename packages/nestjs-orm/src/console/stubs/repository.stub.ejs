import { BaseRepository } from '@nesvel/nestjs-orm';

import { <%= modelName %> } from '@/entities/<%= fileName %>.entity';

/**
 * <%= modelName %> Repository
 *
 * Custom repository for <%= modelName %> entity with additional query methods.
 * To use this repository, register it on the <%= modelName %> entity:
 *
 * @Entity({ repository: () => <%= modelName %>Repository })
 * export class <%= modelName %> extends BaseEntity { ... }
 *
 * @extends BaseRepository<<%= modelName %>>
 * @author Generated by @nesvel/nestjs-orm
 *
 * @see https://mikro-orm.io/docs/repositories#inferring-custom-repository-type
 */
export class <%= modelName %>Repository extends BaseRepository<<%= modelName %>> {
  /**
   * Add custom repository methods here
   *
   * @example
   * async findByEmail(email: string): Promise<<%= modelName %> | null> {
   *   return this.first({ email });
   * }
   *
   * @example
   * async findActive(): Promise<<%= modelName %>[]> {
   *   return this.where({ isActive: true });
   * }
   *
   * @example
   * async findWithRelations(id: number): Promise<<%= modelName %> | null> {
   *   return this.getRepository().findOne(id, {
   *     populate: ['relation1', 'relation2'],
   *   });
   * }
   */
}
