import { Factory } from '@nesvel/nestjs-orm';
import { <%= modelName %> } from '@/entities/<%= fileName %>';

/**
 * <%= name %> Factory
 *
 * @description Factory for generating <%= modelName %> test data
 * @author Generated by @nesvel/nestjs-orm
 */
@Factory(<%= modelName %>)
export class <%= name %> {
  /**
   * Define the default state for the entity
   *
   * @returns Partial entity data for factory generation
   */
  definition(): Partial<<%= modelName %>> {
    return {
      name: 'Sample <%= modelName %>',
      // Add more factory definitions here
    };
  }

  /**
   * Add factory states here
   *
   * @example
   * active(): this {
   *   return this.state({ isActive: true });
   * }
   *
   * @example
   * withEmail(email: string): this {
   *   return this.state({ email });
   * }
   */
}
