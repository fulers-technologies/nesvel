import { config as base } from '@nesvel/eslint-config/base';

/**
 * ESLint configuration array
 *
 * Inherits base linting rules including:
 * - Bundler configuration best practices
 * - Tsup configuration patterns
 * - Build output standards
 * - Module format conventions
 */
export default [...base];
