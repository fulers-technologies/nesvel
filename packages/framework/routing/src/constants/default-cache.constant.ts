/**
 * Default cache time-to-live (TTL) in seconds.
 *
 * Applied when caching is enabled but no specific TTL is provided.
 * This value represents a balance between data freshness and performance.
 *
 * @constant
 * @default 300 seconds (5 minutes)
 */
export const DEFAULT_CACHE_TTL = 300;
