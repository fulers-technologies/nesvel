import { Entity, Property, BaseEntity } from '@nesvel/nestjs-orm';

/**
 * <%= modelName %> Entity
 *
 * @description <%= description || 'Entity description' %>
 * @extends BaseEntity
 * @author Generated by @nesvel/nestjs-orm
 *
 * @remarks
 * To use a custom repository, replace the decorator with:
 * @Entity({ repository: () => <%= modelName %>Repository })
 *
 * This enables type-safe access via em.getRepository(<%= modelName %>) and
 * allows you to add custom query methods to <%= modelName %>Repository.
 *
 * @see https://mikro-orm.io/docs/repositories#inferring-custom-repository-type
 */
@Entity()
export class <%= modelName %> extends BaseEntity {
  /**
   * Type hint for custom repository
   *
   * Uncomment this when using a custom repository to enable type inference:
   * This allows em.getRepository(<%= modelName %>) to return <%= modelName %>Repository
   * instead of EntityRepository<<%= modelName %>>.
   *
   * @see https://mikro-orm.io/docs/repositories#inferring-custom-repository-type
   */
  // [EntityRepositoryType]?: <%= modelName %>Repository;

  /**
   * Name property
   */
  @Property()
  name!: string;

  /**
   * Add your custom properties here
   *
   * @example
   * @Property()
   * email!: string;
   *
   * @Property({ type: 'text', nullable: true })
   * description?: string;
   *
   * @Property({ default: true })
   * isActive!: boolean;
   */

  /**
   * Add relationships here
   *
   * @example
   * @ManyToOne(() => User)
   * user!: User;
   *
   * @OneToMany(() => Post, post => post.author)
   * posts = new Collection<Post>(this);
   */

  /**
   * Add custom methods here
   *
   * @example
   * getFullName(): string {
   *   return `${this.firstName} ${this.lastName}`;
   * }
   */
}
