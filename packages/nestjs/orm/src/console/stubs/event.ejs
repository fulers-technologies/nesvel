import { BaseEvent } from '@nesvel/nestjs-orm';

/**
 * <%= className %> Event
 *
 * Domain event representing when a <%= modelName %> <%= eventAction %>.
 * This event is dispatched to notify other parts of the application
 * about this domain occurrence.
 *
 * **Purpose:**
 * Domain events represent facts about things that have already happened
 * in the domain. They are immutable and can be used to trigger side effects
 * or update read models in event-driven architectures.
 *
 * **Usage:**
 * Dispatch this event when a <%= modelName %> <%= eventAction %>, and handle it
 * in event listeners to perform related actions (e.g., sending notifications,
 * updating analytics, triggering workflows).
 *
 * @description Event emitted when <%= modelName %> <%= eventAction %>
 * @author Generated by @nesvel/nestjs-orm\n * @since 1.0.0
 *
 * @example
 * ```typescript
 * // Using inherited make() method (recommended)
 * const event = <%= className %>.make(
 *   <%= variableName %>.id,
 *   <%= variableName %>.name,
 * ).withMetadata({ correlationId: 'abc-123', userId: user.id });
 *
 * eventEmitter.emit('<%= eventName %>', event);
 *
 * // Listen to the event
 * @OnEvent('<%= eventName %>')
 * async handle<%= className %>(event: <%= className %>) {
 *   this.logger.log(`<%= modelName %> <%= eventAction %>: ${event.<%= variableName %>Id}`);
 *   // Perform side effects
 * }
 * ```
 */
export class <%= className %> extends BaseEvent {
  /**
   * Get the event name
   *
   * Returns the event name for this domain event.
   * Override to provide a custom event name.
   *
   * @returns The event name
   *
   * @description Returns '<%= eventName %>' as the event identifier
   *
   * @example
   * ```typescript
   * const event = <%= className %>.make('123');
   * console.log(event.getEventName()); // '<%= eventName %>'
   * ```
   */
  // public getEventName(): string {
  //   return '<%= eventName %>';
  // }
}
