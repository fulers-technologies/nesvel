/**
 * TypeScript configuration presets
 *
 * This package contains shared TypeScript configurations for the monorepo.
 * These are JSON files that are extended in tsconfig.json files.
 */

// Note: JSON files are not exported as modules
// They are referenced directly in tsconfig.json files via "extends"
